//Area Quadrado

#include <stdio.h>
int main ()
{ int lado,area;
	printf ("Digite um valor para o lado:");
	scanf ("%d",&lado);
	area = lado*lado;
	printf ("O valor da area do quadrado e =");
	printf ("%d", area);
	return 0;
}
